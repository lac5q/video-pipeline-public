---
phase: 02-customer-consent-system
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - scripts/test-consent-flow.js
autonomous: false
requirements:
  - CONS-01
  - CONS-02
  - CONS-03
  - CONS-04
  - CONS-05

must_haves:
  truths:
    - "Consent email renders correctly with brand colors, illustration, and 'Yes, share my art!' CTA"
    - "Consent server starts and serves landing pages at /consent/:token endpoints"
    - "Approve flow: token click -> consent_status updated to 'approved' -> coupon code shown on page"
    - "Decline flow: token click -> consent_status updated to 'denied' -> respectful 'no problem' page"
    - "send-consent-batch.js sends emails for orders with consent_status='pending' and customer_email populated"
    - "approve-consent-candidates.js correctly transitions consent_status from pre_approved to pending"
    - "Consent state persists across restarts and is queryable via list-orders.js"
  artifacts:
    - path: "scripts/test-consent-flow.js"
      provides: "End-to-end consent flow smoke test"
      min_lines: 60
  key_links:
    - from: "scripts/approve-consent-candidates.js"
      to: "scripts/send-consent-batch.js"
      via: "consent_status='pending' enables batch sender to pick up orders"
      pattern: "consent_status.*pending"
    - from: "scripts/send-consent-batch.js"
      to: "scripts/consent-server.js"
      via: "email links point to consent server URLs"
      pattern: "CONSENT_BASE_URL"
    - from: "scripts/consent-server.js"
      to: "lib/consent.js"
      via: "validateConsentToken -> updateConsent -> markTokenUsed"
      pattern: "updateConsent.*approved"
---

<objective>
Create an automated smoke test that validates the full consent flow end-to-end (without sending real emails or hitting real Shopify), then verify with Luis that emails render correctly and the consent server pages look right.

Purpose: All components from Plans 01 and 02 must work together as a pipeline: approve -> enrich -> send email -> customer clicks -> consent updated -> coupon shown. This plan validates wiring between all components.

Output: test-consent-flow.js smoke test script, visual verification of email template and consent server pages.
</objective>

<execution_context>
@/Users/lcalderon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lcalderon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-customer-consent-system/02-CONTEXT.md
@.planning/phases/02-customer-consent-system/02-RESEARCH.md
@.planning/phases/02-customer-consent-system/02-01-SUMMARY.md
@.planning/phases/02-customer-consent-system/02-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01 and 02 -->

From lib/consent.js:
```javascript
function getConsentStatus(orderId, brand) // { order_id, brand, consent_status, ... }
function updateConsent(orderId, brand, newStatus, details) // { changes }
function listPendingConsent(brand) // Array of pending orders
function generateConsentToken(orderId, brand) // { approveToken, denyToken }
function validateConsentToken(token) // { orderId, brand, action } | null
function markTokenUsed(token)
function getDb() // better-sqlite3 instance
```

From lib/email.js:
```javascript
function sendConsentRequest(orderId, brand, customerEmail, customerName, orderDescription, options)
function renderTemplate(templatePath, vars) // now includes ILLUSTRATION_URL
function loadBrandConfig(brandSlug)
```

From scripts/consent-server.js:
```javascript
// Express app exported as module.exports = app
// GET /consent/:token -- shows branded landing page with illustration + "Yes, share my art!"
// GET /consent/:token/confirm -- finalizes approval, creates Shopify coupon (GraphQL), shows coupon on page
// GET /consent/:token/decline -- marks as denied, shows "no problem" page
// GET /status -- health check
```

DB schema:
- orders: order_id, brand, consent_status, customer_email, customer_name, oms_url, illustration_id
- consent_tokens: token, order_id, brand, action, created_at, used_at, expires_at
- consent_log: id, order_id, brand, action, old_status, new_status, details, timestamp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create end-to-end consent flow smoke test</name>
  <files>
    scripts/test-consent-flow.js
  </files>
  <action>
    Create `scripts/test-consent-flow.js` that validates the full consent pipeline without sending real emails or hitting real APIs.

    The script should:

    1. **Setup**: Create a temporary test DB (use `DB_PATH` env override pointing to `/tmp/test-consent-flow.db`). Initialize schema. Insert 3 test orders:
       - Order "TEST-001" brand "turnedyellow", consent_status "pre_approved", customer_email "test@example.com", customer_name "Test User", oms_url "https://doh.turnedyellow.com/customer/illustration/test123"
       - Order "TEST-002" brand "makemejedi", consent_status "pre_approved", customer_email null (no email -- should be skipped by batch sender)
       - Order "TEST-003" brand "turnedyellow", consent_status "pre_approved", customer_email "test2@example.com", customer_name "Another User"

    2. **Test consent state transition** (CONS-02):
       - Call `consent.updateConsent('TEST-001', 'turnedyellow', 'pending', 'test approval')`
       - Verify `getConsentStatus` returns consent_status='pending'
       - Verify consent_log has an entry with action='consent_pending'
       - Print PASS/FAIL

    3. **Test token generation and validation** (CONS-04):
       - Call `consent.generateConsentToken('TEST-001', 'turnedyellow')`
       - Verify both tokens are 64-char hex strings
       - Call `validateConsentToken(approveToken)` -- should return { orderId: 'TEST-001', brand: 'turnedyellow', action: 'approve' }
       - Call `markTokenUsed(approveToken)`
       - Call `validateConsentToken(approveToken)` again -- should return null (used)
       - Print PASS/FAIL

    4. **Test email template rendering** (CONS-01):
       - Call `email.renderTemplate('templates/consent-email.html', { ...testVars })` with all required vars including ILLUSTRATION_URL
       - Verify rendered HTML contains "Yes, share my art"
       - Verify rendered HTML contains the illustration URL
       - Verify rendered HTML does NOT contain "social media" or "marketing materials"
       - Verify rendered HTML contains "TurnedYellow" (brand name substituted)
       - Save rendered HTML to `/tmp/test-consent-email.html` for visual inspection
       - Print PASS/FAIL

    5. **Test consent server routes** (CONS-04):
       - Start the consent server on a random port (use `app.listen(0)`)
       - Generate fresh tokens for TEST-003
       - HTTP GET `http://localhost:{port}/consent/{approveToken}` -- verify 200 status and body contains "share my art"
       - HTTP GET `http://localhost:{port}/consent/{approveToken}/confirm` -- verify 200 status and body contains "Thank"
       - Verify TEST-003 consent_status is now 'approved' in DB
       - HTTP GET `http://localhost:{port}/status` -- verify 200 and JSON contains "ok"
       - Stop server
       - Print PASS/FAIL

    6. **Test batch sender skips missing emails** (CONS-01):
       - Set TEST-002 to consent_status='pending'
       - Call `consent.listPendingConsent('makemejedi')` -- verify TEST-002 is returned
       - Verify TEST-002 has null customer_email (would be skipped by send-consent-batch.js)
       - Print PASS/FAIL

    7. **Cleanup**: Delete temp DB, print summary (X/Y tests passed)

    Use `require('http')` for making test requests to the consent server (not external test libs).
    Use `process.env.DB_PATH = '/tmp/test-consent-flow.db'` BEFORE requiring lib modules.
    Print clear PASS/FAIL for each test section with a final summary.
  </action>
  <verify>
    DB_PATH=/tmp/test-consent-flow.db node scripts/test-consent-flow.js
    # Should show all tests passing. Check for "X/X tests passed" in output.
  </verify>
  <done>
    test-consent-flow.js runs all 6 test sections. All pass. Temporary test DB is cleaned up. Rendered email HTML saved to /tmp/test-consent-email.html for visual check.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of consent email and server pages</name>
  <files>none</files>
  <action>
    Present the following verification steps to Luis for visual confirmation of the consent system.

    What was built across Plans 01, 02, and 03:
    - Customer data enrichment script (enrich-orders.js)
    - Consent batch approval CLI (approve-consent-candidates.js) with list + drill-down modes
    - Branded consent email with illustration preview and "Yes, share my art!" CTA
    - Consent server with branded landing pages and GraphQL Shopify coupon creation
    - End-to-end smoke test validating the full flow

    Steps for Luis to verify:

    1. Open /tmp/test-consent-email.html in a browser and confirm:
       - Email looks branded (dark background header, accent-colored CTA button)
       - Illustration preview image placeholder is visible
       - CTA says "Yes, share my art!" (NOT "feature my order")
       - Tone is warm and personal, no corporate language about social media/marketing materials
       - Decline link says "No thanks" and is subtle (not equal weight to approve)

    2. Start consent server and check landing pages:
       DB_PATH=/tmp/test-consent-flow.db node scripts/consent-server.js
       Then visit a token URL in browser to check:
       - Brand colors and name
       - Illustration image
       - "Yes, share my art!" button (prominent)
       - "No thanks" link (subtle)

    3. Run the approval CLI to verify UX:
       node scripts/approve-consent-candidates.js --list --brand turnedyellow --limit 5
       Verify: table output shows ranked candidates with expected fields
  </action>
  <verify>Luis confirms visual output is acceptable by typing "approved"</verify>
  <done>Luis has visually verified email template, consent server landing pages, and approval CLI output</done>
</task>

</tasks>

<verification>
1. `DB_PATH=/tmp/test-consent-flow.db node scripts/test-consent-flow.js` -- all tests pass
2. `/tmp/test-consent-email.html` renders correctly in browser
3. `node scripts/approve-consent-candidates.js --help` runs without error
4. `grep -r "price_rules" scripts/consent-server.js` returns 0 matches
5. `node scripts/list-orders.js --brand turnedyellow --limit 3` shows orders with consent data
</verification>

<success_criteria>
- Smoke test passes all 6 sections end-to-end
- Email renders with correct CTA, illustration, warm tone (verified visually by Luis)
- Consent server landing pages look correct (verified visually by Luis)
- Full pipeline flow validated: pre_approved -> pending (via CLI) -> email sent -> token clicked -> approved -> coupon shown
- Consent state persists and is queryable
</success_criteria>

<output>
After completion, create `.planning/phases/02-customer-consent-system/02-03-SUMMARY.md`
</output>
