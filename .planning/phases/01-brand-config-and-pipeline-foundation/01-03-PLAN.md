---
phase: 01-brand-config-and-pipeline-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - scripts/regression-test.sh
autonomous: false
requirements:
  - QUAL-02
  - QUAL-04
  - QUAL-05

must_haves:
  truths:
    - "The regression test script runs the new pipeline on TY-133627 and compares output against structural baselines (duration, resolution, codec, fps)"
    - "No hardcoded /Users/lcalderon/ paths exist anywhere in scripts/ or produce-video.sh (verified by grep)"
    - "The new pipeline produces a video file that passes verify-video.sh (1080x1920, h264, CRF 18, 30fps)"
    - "Luis has visually confirmed the new pipeline output looks correct for TY-133627"
    - "Old build scripts in orders/133627/exports/ and orders/130138/exports/ are byte-identical to git HEAD"
  artifacts:
    - path: "scripts/regression-test.sh"
      provides: "Automated regression test comparing new pipeline output against baseline properties"
      min_lines: 40
  key_links:
    - from: "scripts/regression-test.sh"
      to: "produce-video.sh"
      via: "invokes the new pipeline for TY-133627"
      pattern: "produce-video.sh.*--brand.*turnedyellow"
    - from: "scripts/regression-test.sh"
      to: "scripts/verify-video.sh"
      via: "runs quality gate on output"
      pattern: "verify-video.sh"
---

<objective>
Validate the new parameterized pipeline produces correct output by running regression tests against the known TY-133627 order and getting Luis's visual confirmation.

Purpose: The whole point of Phase 1 is "no regressions." The proven TY pipeline produces excellent videos. The new parameterized pipeline must produce output of equal or better quality. This plan proves it by running the new scripts on a known order and checking every quality dimension -- structural (automated) and visual (human).

Output: `scripts/regression-test.sh` automated checker, regression test results, visual confirmation from Luis.
</objective>

<execution_context>
@/Users/lcalderon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lcalderon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-brand-config-and-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-brand-config-and-pipeline-foundation/01-RESEARCH.md
@.planning/phases/01-brand-config-and-pipeline-foundation/01-01-SUMMARY.md
@.planning/phases/01-brand-config-and-pipeline-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create regression test script and run automated checks</name>
  <files>
    scripts/regression-test.sh
  </files>
  <action>
Create `scripts/regression-test.sh` that validates the new pipeline against the TY-133627 baseline.

The script performs these checks:

**1. Hardcoded Path Scan (BRAND-05):**
```bash
echo "=== Checking for hardcoded paths ==="
HARDCODED=$(grep -rn '/Users/lcalderon' produce-video.sh scripts/ 2>/dev/null | wc -l)
if [ "$HARDCODED" -gt 0 ]; then
    echo "FAIL: Found $HARDCODED hardcoded /Users/lcalderon/ references"
    grep -rn '/Users/lcalderon' produce-video.sh scripts/
    exit 1
fi
echo "PASS: No hardcoded paths"
```

**2. Config Completeness Check:**
- Verify all 5 brand configs parse as valid JSON
- Verify products.json has the expected 17 products
- Verify each brand config has all required fields (name, slug, colors, logo, cta, etc.)

**3. Old Scripts Integrity (QUAL-04):**
- Check that orders/133627/exports/build-ugc-v11.sh exists and is unchanged from git HEAD
- Check that orders/130138/exports/build-ugc-v1.sh exists and is unchanged from git HEAD
- Use `git diff HEAD -- orders/` to verify zero changes

**4. Pipeline Dry Run (structural only):**
- Run `./produce-video.sh --brand turnedyellow --order 133627 --skip-build` to test config loading and workspace setup
- Verify workspace directory created at orders/turnedyellow/133627/
- Verify all config values loaded correctly (echo them)

**5. Video Build (if assets available):**
- Check if staged mockups exist at `orders/133627/mockups/v11_*.png` (they may be in the old path structure)
- If assets exist: run full build and compare output with verify-video.sh
- If assets not available: log that manual build test is needed and skip

**6. Output Comparison (if baseline video exists):**
Per RESEARCH.md regression methodology:
- Duration: within 0.5s of original (if original exists for comparison)
- Resolution: exact 1080x1920
- Codec: h264
- FPS: ~30

The script MUST:
- Use `set -euo pipefail`
- Be executable (chmod +x)
- Print clear PASS/FAIL for each check
- Exit 0 only if ALL checks pass
- Use `${PIPELINE_ROOT}` for all paths

After creating the script, RUN it:
```bash
cd /Users/lcalderon/github/video-pipeline && bash scripts/regression-test.sh
```

Fix any failures found. The automated checks MUST all pass before proceeding to the visual checkpoint.
  </action>
  <verify>
    <automated>cd /Users/lcalderon/github/video-pipeline && bash scripts/regression-test.sh</automated>
  </verify>
  <done>
    - scripts/regression-test.sh exists and is executable
    - All automated checks pass: no hardcoded paths, configs valid, old scripts untouched
    - Pipeline dry run succeeds (config loads, workspace created)
    - If assets available: video builds and passes verify-video.sh
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of pipeline output</name>
  <files>None (verification only)</files>
  <action>
    Present regression test results to Luis for visual verification. The automated checks from Task 1 must all pass before this checkpoint. Luis reviews:
    1. Regression test output (all PASS)
    2. Video output quality (if produced) compared to original TY-133627
    3. TurnedComics and PopSmiths placeholder config values
    4. Overall satisfaction with parameterized scripts
  </action>
  <verify>Luis confirms "approved" after reviewing output</verify>
  <done>Luis has approved the pipeline output quality and config values</done>
  <what-built>
    The new parameterized pipeline scripts that should produce videos identical in quality to the proven build-ugc-v11.sh. All automated regression checks have passed.
  </what-built>
  <how-to-verify>
    1. Review the regression test output above -- all automated checks should show PASS
    2. If a video was produced for TY-133627:
       - Open it and compare against the published TY-133627 video
       - Check: hook text readable and correct colors? Reaction segment with label? Products with labels? Logo end card with correct CTA?
       - Check: no black bars, no stretching, no distortions on any product?
    3. If no video was produced (assets not in expected location):
       - Review the dry run output showing config values loaded correctly
       - Confirm the config values for TurnedYellow match the original .conf values
       - We can do a full build test manually once assets are staged
    4. Review brands/turnedcomics.json and brands/popsmiths.json placeholder values -- are the colors/taglines reasonable starting points?
    5. Confirm: are you satisfied with the parameterized scripts, or do any values need adjustment?
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `bash scripts/regression-test.sh` exits 0 with all PASS results
- `grep -rn '/Users/lcalderon' produce-video.sh scripts/` returns empty (zero matches)
- `git diff HEAD -- orders/133627/ orders/130138/` returns empty (old scripts untouched)
- Luis approves visual quality (checkpoint)
</verification>

<success_criteria>
1. All automated regression checks pass
2. No hardcoded paths anywhere in new scripts
3. Old TY build scripts verified unchanged
4. Luis approves pipeline output quality (or confirms dry run config values are correct)
5. Phase 1 success criteria from ROADMAP.md are met:
   - Brand config drives video build (not hardcoded values)
   - No hardcoded /Users/lcalderon/ paths in scripts
   - OMS-correct product parameters in catalog
   - Video specs enforced (1080x1920, CRF 18, h264, 30fps)
   - Existing TY scripts preserved as backup
</success_criteria>

<output>
After completion, create `.planning/phases/01-brand-config-and-pipeline-foundation/01-03-SUMMARY.md`
</output>
