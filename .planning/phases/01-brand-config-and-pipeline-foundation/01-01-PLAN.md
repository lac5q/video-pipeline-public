---
phase: 01-brand-config-and-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brands/turnedyellow.json
  - brands/makemejedi.json
  - brands/turnedwizard.json
  - brands/turnedcomics.json
  - brands/popsmiths.json
  - products.json
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - BRAND-01
  - QUAL-01
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "Each of the 5 brands has a JSON config file with all required fields (name, slug, colors, logo, cta, hook_templates, music_pool, product_showcase_order, oms_app)"
    - "A single products.json defines all 17 products with OMS-correct position parameters (450x450 apparel, orientation-aware wall art, rotated phone cases, front-view mug)"
    - "Product showcase orders exist for both 'default' and 'home_decor_first' (PopSmiths) display sequences"
    - "No secrets (API keys, tokens) appear in any JSON config file -- only in .env.example as documentation"
    - "Existing TY build scripts in orders/133627/exports/ and orders/130138/exports/ are completely untouched"
  artifacts:
    - path: "brands/turnedyellow.json"
      provides: "TurnedYellow brand config with values from existing .conf file"
      contains: "TurnedYellow"
    - path: "brands/makemejedi.json"
      provides: "MakeMeJedi brand config with values from existing .conf file"
      contains: "MakeMeJedi"
    - path: "brands/turnedwizard.json"
      provides: "TurnedWizard brand config with values from existing .conf file"
      contains: "TurnedWizard"
    - path: "brands/turnedcomics.json"
      provides: "TurnedComics brand config with placeholder values for Luis to review"
      contains: "TurnedComics"
    - path: "brands/popsmiths.json"
      provides: "PopSmiths brand config with home_decor_first showcase order"
      contains: "PopSmiths"
    - path: "products.json"
      provides: "Shared product catalog with all 17 products and OMS-correct parameters"
      contains: "showcase_orders"
    - path: ".env.example"
      provides: "Documentation of required environment variables"
      contains: "GEMINI_API_KEY"
  key_links:
    - from: "brands/*.json"
      to: "products.json"
      via: "product_showcase_order field references showcase_orders keys"
      pattern: "product_showcase_order.*default|home_decor_first"
---

<objective>
Create the brand config system and shared product catalog that all downstream pipeline scripts will read from.

Purpose: This is the data foundation for Phase 1. Every brand-specific value (colors, logos, CTA text, hook templates) and every product parameter (Printful IDs, positions, staging prompts) gets defined here as the single source of truth. Without these configs, the parameterized build scripts in Plan 02 have nothing to read.

Output: 5 brand JSON configs in `brands/`, 1 shared `products.json`, `.env.example` for secrets documentation, updated `.gitignore`.
</objective>

<execution_context>
@/Users/lcalderon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lcalderon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-brand-config-and-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-brand-config-and-pipeline-foundation/01-RESEARCH.md
@docs/PRODUCT-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brand JSON configs for all 5 brands</name>
  <files>
    brands/turnedyellow.json
    brands/makemejedi.json
    brands/turnedwizard.json
    brands/turnedcomics.json
    brands/popsmiths.json
  </files>
  <action>
Create one JSON config file per brand in `brands/` directory. Use the schema from RESEARCH.md Pattern 1.

**TurnedYellow** (from existing .conf):
- name: "TurnedYellow", slug: "turnedyellow", url: "TurnedYellow.com"
- tagline: "Your Photo. Your Style. Your Products.", style_description: "Hand-illustrated, one-of-a-kind"
- colors: { background: "#1a1a2e", accent: "#FF8C00", hook_accent: "#FFD700", label_brand: "rgba(255,200,100,0.9)" }
- logo: { file: "turnedyellow-white.png", width: 600 }
- cta: { line1: "Shop our collections", line2_url: "TurnedYellow.com", line3_tagline: "Hand-illustrated, one-of-a-kind" }
- hook_templates: at least 2 templates from existing build scripts. First: beat1="this is what happens", beat2="when a customer\nopens our gift"
- music_pool: ["Tobu - Candyland [Privated NCS Release].mp3", "Ehrling - Dance With Me.mp3"]
- product_showcase_order: "default"
- reaction_label: "Real customer reaction"
- font: "/System/Library/Fonts/HelveticaNeue.ttc" (from existing scripts, can be overridden per brand)
- drive_folder_ids: {} (empty placeholder -- not secret per research, can live in config)
- oms_app: "turnedyellowordermanagement"

**MakeMeJedi** (from existing .conf):
- name: "MakeMeJedi", slug: "makemejedi", url: "MakeMeJedi.com"
- tagline: "May the art be with you", style_description: "Jedi-style illustration art"
- colors: { background: "#000814", accent: "#00B8FF", hook_accent: "#FFE81F", label_brand: "#FFE81F" }
- logo: { file: "makemejedi-white.png", width: 400 }
- cta: { line1: "Commission your portrait", line2_url: "MakeMeJedi.com", line3_tagline: "May the art be with you" }
- hook_templates: beat1="From selfie to", beat2="Jedi Master"
- product_showcase_order: "default"
- oms_app: "makemejedibuyonce"

**TurnedWizard** (from existing .conf):
- name: "TurnedWizard", slug: "turnedwizard", url: "TurnedWizard.com"
- tagline: "Your portrait, conjured in magic", style_description: "Wizard-style illustration art"
- colors: { background: "#1A0030", accent: "#9B59B6", hook_accent: "#FFD700", label_brand: "#FFD700" }
- logo: { file: "turnedwizard-white.png", width: 500 }
- cta: { line1: "Get your wizard portrait", line2_url: "TurnedWizard.com", line3_tagline: "Your portrait, conjured in magic" }
- hook_templates: beat1="Your Hogwarts portrait", beat2="just arrived"
- product_showcase_order: "default"
- oms_app: "turnedwizardmanage"

**TurnedComics** (PLACEHOLDER -- needs Luis review):
- name: "TurnedComics", slug: "turnedcomics", url: "TurnedComics.com"
- tagline: "Your photo, comic-style", style_description: "Comic book illustration art"
- colors: { background: "#1a1a2e", accent: "#E63946", hook_accent: "#FFBE0B", label_brand: "#FFBE0B" }
- logo: { file: "turnedcomics-white.png", width: 500 }
- Add comment in file or note that values are placeholders
- product_showcase_order: "default"
- oms_app: "turnedcomicsmanage"

**PopSmiths** (PLACEHOLDER -- needs Luis review):
- name: "PopSmiths", slug: "popsmiths", url: "PopSmiths.com"
- tagline: "Art for your home", style_description: "Custom framed art and home decor"
- colors: { background: "#2C3E50", accent: "#E67E22", hook_accent: "#F39C12", label_brand: "#F39C12" }
- logo: { file: "popsmiths-white.png", width: 500 }
- product_showcase_order: "home_decor_first" (CRITICAL: PopSmiths uses different display order per user decision)
- oms_app: null (PopSmiths uses Heroku server, not shared OMS)

All configs MUST NOT contain any secrets (API keys, tokens). Secrets go in env vars.
Drive folder IDs CAN live in config (not truly secret per research recommendation).
  </action>
  <verify>
    <automated>cd /Users/lcalderon/github/video-pipeline && for f in brands/*.json; do echo "=== $f ===" && jq '.' "$f" > /dev/null && echo "Valid JSON" && jq -r '.name, .slug, .colors.background, .product_showcase_order' "$f"; done && echo "=== Count ===" && ls brands/*.json | wc -l</automated>
  </verify>
  <done>
    - 5 valid JSON files exist in brands/
    - Each has all required fields: name, slug, url, tagline, colors (background, accent, hook_accent, label_brand), logo (file, width), cta (line1, line2_url, line3_tagline), hook_templates, music_pool, product_showcase_order, reaction_label, font, drive_folder_ids, oms_app
    - TY/MMJ/TW values match their existing .conf files exactly
    - TC and PopSmiths marked as placeholders
    - PopSmiths has product_showcase_order: "home_decor_first"
    - No secrets in any config file
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared product catalog and environment config</name>
  <files>
    products.json
    .env.example
    .gitignore
  </files>
  <action>
**products.json** -- Create the shared product catalog with ALL 17 products from PRODUCT-CATALOG.md. Use the schema from RESEARCH.md Pattern 2.

Each product entry MUST include:
- id (slug), label (display name), provider (printful/gooten)
- product_id, variant_id (from PRODUCT-CATALOG.md exact values)
- placement, fill_mode (where applicable)
- orientation_aware: true for wall art (framed_poster, canvas, poster) -- these need landscape/portrait position variants
- position or position_landscape/position_portrait with EXACT OMS-correct values from PRODUCT-CATALOG.md
- For apparel (tshirt, hoodie, sweatshirt, tanktop): position MUST be 450x450 square. NEVER 1800x2400.
- For phone cases (iphone_case, samsung_case): note that landscape illustrations need 270-degree rotation pre-processing
- For mug: options MUST include "Front view"
- staging_prompt: descriptive prompt for Gemini staging. Blanket MUST say "draped flat on a bed" (never standing upright)
- video_include: true/false (sticker, puzzle, greeting_card, ornament = false or lower priority)
- default_order and home_decor_order: numeric position for showcase ordering
- option_groups where applicable (Wrinkled for tshirt, On Hanger for hoodie)

Products to include (all 17 from PRODUCT-CATALOG.md):
1. framed_poster (Printful 2/4, orientation-aware, cover)
2. canvas (Printful 3/5, orientation-aware, cover)
3. poster (Printful 1/2, orientation-aware, cover)
4. tshirt (Printful 71/4013, 450x450, Wrinkled)
5. hoodie (Printful 146/5524, 450x450, On Hanger)
6. sweatshirt (Printful 145/5428, 450x450)
7. tanktop (Printful 248/8661, 450x450)
8. mug (Printful 19/1320, 1550x1050, Front view)
9. waterbottle (Printful 382/10798, 2557x1582)
10. iphone_case (Printful 181/10994, 879x1830, rotate landscape)
11. samsung_case (Printful 267/11347, 936x1950, rotate landscape)
12. totebag (Printful 367/10458, 1500x1500, placement=front)
13. sticker (Printful 358/10163, 1275x1275)
14. puzzle (Printful 534/13431, 4200x3300)
15. greeting_card (Printful 568/14457, 1842x1240, placement=front)
16. blanket (Gooten, MAXFIT, staging: draped on bed)
17. ornament (Gooten, MAXFIT, crop coords)

Include `showcase_orders` object with two keys:
- "default": ["framed_poster", "canvas", "tshirt", "hoodie", "sweatshirt", "tanktop", "mug", "waterbottle", "iphone_case", "totebag", "blanket", "poster"] (the standard 12 from PRODUCT-CATALOG.md)
- "home_decor_first": ["canvas", "framed_poster", "poster", "blanket", "mug", "tshirt", "hoodie", "sweatshirt", "tanktop", "waterbottle", "iphone_case", "totebag"] (PopSmiths order per user decision)

**.env.example** -- Document all required environment variables:
```
# Required API keys
PRINTFUL_API_KEY=          # Printful API token
GOOTEN_RECIPEID=           # Gooten recipe ID for blanket/ornament
GEMINI_API_KEY=            # Google Gemini API key for staging

# AWS/Wasabi S3 (illustration upload for Printful API)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=             # Wasabi bucket name
AWS_S3_ENDPOINT=           # Wasabi endpoint URL

# Optional
PIPELINE_ROOT=             # Override auto-detected pipeline root
MUSIC_DIR=                 # Override default brands/assets/music/ location
```

**.gitignore** -- Read existing .gitignore if present. ADD (don't replace) these entries:
- `brands/assets/music/*.mp3` (large binary files)
- `orders/*/tmp_*` (temp build files)
- `.env` (secrets)
- Do NOT gitignore brands/*.json or products.json (these are config, not secrets)

CRITICAL: Do NOT modify anything in orders/133627/ or orders/130138/ -- QUAL-04 requires existing scripts remain untouched.
  </action>
  <verify>
    <automated>cd /Users/lcalderon/github/video-pipeline && jq '.' products.json > /dev/null && echo "Valid JSON" && echo "Products: $(jq '.products | length' products.json)" && echo "Showcase default: $(jq '.showcase_orders.default | length' products.json)" && echo "Showcase home_decor: $(jq '.showcase_orders.home_decor_first | length' products.json)" && echo "Apparel 450x450 check:" && jq '.products[] | select(.id == "tshirt" or .id == "hoodie" or .id == "sweatshirt" or .id == "tanktop") | "\(.id): \(.position.area_width)x\(.position.area_height)"' products.json && echo "Mug front view:" && jq '.products[] | select(.id == "mug") | .options' products.json && echo "Env example exists:" && test -f .env.example && echo "YES" && echo "Old scripts untouched:" && test -f orders/133627/exports/build-ugc-v11.sh && test -f orders/130138/exports/build-ugc-v1.sh && echo "YES"</automated>
  </verify>
  <done>
    - products.json has all 17 products with exact OMS-correct parameters from PRODUCT-CATALOG.md
    - All apparel products use 450x450 position (not 1800x2400)
    - Mug has "Front view" option
    - Phone cases note landscape rotation requirement
    - Blanket staging prompt includes "draped flat on a bed"
    - Both showcase_orders defined (default=12 products, home_decor_first=12 products)
    - .env.example documents all required env vars without actual values
    - .gitignore updated with music, temp, and .env exclusions
    - orders/133627/ and orders/130138/ completely untouched
  </done>
</task>

</tasks>

<verification>
- `jq '.' brands/*.json products.json` all parse without errors
- `jq -r '.product_showcase_order' brands/popsmiths.json` returns "home_decor_first"
- `jq '.products[] | select(.id == "tshirt") | .position.area_width' products.json` returns 450
- `jq '.products[] | select(.id == "mug") | .options' products.json` includes "Front view"
- `jq '.products[] | select(.id == "blanket") | .staging_prompt' products.json` contains "draped"
- `grep -r 'API_KEY\|SECRET\|TOKEN' brands/` returns no matches (no secrets in configs)
- `diff orders/133627/exports/build-ugc-v11.sh` unchanged from git HEAD
- `ls brands/*.json | wc -l` returns 5
</verification>

<success_criteria>
1. All 5 brand configs are valid JSON and contain every required field
2. Product catalog has all 17 products with verified OMS-correct parameters
3. No secrets exist in any config file
4. Existing TY build scripts are untouched
5. PopSmiths config uses home_decor_first showcase order
</success_criteria>

<output>
After completion, create `.planning/phases/01-brand-config-and-pipeline-foundation/01-01-SUMMARY.md`
</output>
